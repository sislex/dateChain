/**
 * Typed API client surface. `schema.ts` is generated from the backend OpenAPI
 * spec (`pnpm --filter @datechain/api-client gen:types`) and re-exported here.
 * RTK Query endpoints will be added on top of these types in Phase 4.
 */
export type { paths, components, operations } from "./schema";
